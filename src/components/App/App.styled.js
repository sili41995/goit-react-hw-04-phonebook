import styled from '@emotion/styled';

const Container = styled.div`
  width: 500px;
  padding: 50px;
  display: flex;
  flex-direction: column;
  gap: 20px;
`;

const Title = styled.h1``;

const Subtitle = styled.h1``;

const css = { Container, Title, Subtitle };

export default css;
